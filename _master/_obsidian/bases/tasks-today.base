filters:
  and:
    - file.hasTag("task")
    - or:
        - scheduled == today()
        - due == today()
        - status == "in-progress"
views:
  - type: table
    name: Today
    order:
      - file.name
      - status
      - priority
      - scheduled
      - due
      - contexts
