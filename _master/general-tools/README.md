# General Tools

Reusable tools outside the `vault` dispatcher.

Read next:

- [[_master/general-tools/README-tools|Tooling SOP]] for tooling SOPs.
- [[_master/general-tools/invoice-generation/README|Invoice Generation]] for invoice generation.
- Each tool folder's own `README.md` before running scripts there.

Use `_master/system/scripts/` only when a command belongs behind `vault`.
