# Update Financial Model From Statements Config

Private active paths, import window, currency, expected counts, account IDs, owner/family match patterns, reviewed recurring commitments, and Witan executable for `update-financial-model-from-statements`.

Edit `private/workflow.json` when instance locations, identity patterns, reviewed commitments, or active window change. Reusable taxonomy and generic classification rules remain skill behavior.
