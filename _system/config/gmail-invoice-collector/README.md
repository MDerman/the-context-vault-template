# Gmail Invoice Collector Config

Private paths and Google Cloud setup identity for `gmail-invoice-collector`.

`private/config.toml` is default collector config. It owns invoice/statement roots, report root, candidate query, and OAuth setup project. OAuth credentials remain GWS-owned outside vault.

