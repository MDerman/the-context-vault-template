interface:
  display_name: "Live View Publisher"
  short_description: "Publish Markdown, PRs, overviews, and plans"
  default_prompt: "Use $live-view-plan to publish this artifact online without changing its document type."
policy:
  allow_implicit_invocation: true
